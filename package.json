{
  "name": "ci-on-demand",
  "version": "1.2.2",
  "private": true,
  "description": "Composite action: commit-status reporting, ci:* label release, and a PR run-history table for on-demand CI",
  "scripts": {
    "test": "node test/history.test.js && node test/update.test.js"
  },
  "license": "MIT"
}
